# Security App JS

## Lancer le projet

1. Copier la config : `cp .env.example .env` puis personnaliser `SESSION_SECRET` si besoin.
2. Installer les dépendances : `npm install`
3. Démarrer le serveur : `npm start`
4. Ouvrir le navigateur sur `http://localhost:3001/login`
